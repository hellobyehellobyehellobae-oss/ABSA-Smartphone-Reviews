# Process of storing data after each project step.
## After each running job (aspect extraction,high/main category) data for each job is stored under name results_task_{task_index}.csv, so that it is possible to automatically merge dataset running one script(merging folder)
